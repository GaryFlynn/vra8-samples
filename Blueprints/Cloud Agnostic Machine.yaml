formatVersion: 1
inputs:
  image:
    type: string
    enum:
      - ubuntu18
      - windows2019
      - rhel8
  flavor:
    type: string
    enum:
      - large
      - medium
      - small
resources:
  Cloud_Machine_1:
    type: Cloud.Machine
    metadata:
      layoutPosition:
        - 0
        - 0
    properties:
      image: '${input.image}'
      flavor: '${input.flavor}'
